# ZeroGPU Gradio Space requirements (pinned to a known-good combo).
# gradio 4.44.x imports HfFolder from huggingface_hub, which newer hub releases
# removed — so huggingface_hub is pinned to the 0.25.x line. Keep these in sync.
gradio==4.44.1
huggingface_hub==0.25.2
spaces==0.30.4
torch
pillow
numpy
imageio-ffmpeg
requests
pydantic>=2,<3
pydantic-settings>=2,<3

# ZeroGPU Gradio Space — extra deps only.
# gradio itself is installed by HF from `sdk_version` in README.md (5.50.0),
# which manages its own matching gradio_client / huggingface_hub.
spaces
torch
pillow
edge-tts
numpy
imageio-ffmpeg
requests
pydantic>=2,<3
pydantic-settings>=2,<3

# In-process Wav2Lip lip-sync (talking-face motion)
opencv-python-headless
librosa
soundfile

# GFPGAN face restoration (sharpens the generated mouth/face)
gfpgan
basicsr
facexlib

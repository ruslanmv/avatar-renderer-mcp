# ZeroGPU Gradio Space requirements.
# Kept lean so the Space builds reliably. The guaranteed render path (ffmpeg)
# needs no ML deps; torch is present so the @spaces.GPU function runs real GPU
# code once an in-process model is wired in.
gradio==4.44.1
spaces
torch
pillow
numpy
imageio-ffmpeg
requests
pydantic>=2,<3
pydantic-settings>=2,<3
huggingface_hub
